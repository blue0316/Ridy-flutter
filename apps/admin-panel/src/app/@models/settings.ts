export default class Settings {
    firebaseKeyFile?: string;
    googleMapsKey?: string;
    purchaseCode?: string;
    version?: number;
}

export class MySqlSettings {
    host = 'locahost';
    user = 'root';
    password = '';
    database = 'taxinew';
    port = 3306;
}